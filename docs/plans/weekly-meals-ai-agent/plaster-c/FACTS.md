# Plaster C — fakty ze zwiadu (28.08.2026, gałąź develop = plaster B)

## Platforma (C1–C4, C9)
- `src/main.ts` (32 linie): CORS z `CORS_ORIGIN` + zaszyty `http://localhost:5173`, `ValidationPipe {whitelist, forbidNonWhitelisted, transform}`, static `/static/`, `listen(PORT ?? 3000)`; brak `enableShutdownHooks`, brak walidacji env, brak filtra, `bootstrap()` bez `.catch`.
- `src/auth/auth.module.ts:13` `JWT_SECRET ?? 'dev-secret-change-me'`; `auth.service.ts:46-47` pepper → `JWT_SECRET` → `'dev-pepper'`; `auth.service.ts:174` dev-login opt-out (`=== 'false'`); `auth.service.ts:55-80` `loginWithGoogle` bez weryfikacji tokenu; `auth.controller.ts:14-25` `POST /auth/google`.
- `src/integrations/cookidoo-service.client.ts:20-26` `COOKIDOO_SERVICE_TOKEN ?? ''`; jedyny fail-fast: `src/common/crypto.util.ts:13-21` `parseEncryptionKey` (inicjalizator pola w `cookidoo-integration.service.ts:33-36`).
- Brak `src/config/`, `@nestjs/config`, joi/zod. `@nestjs/throttler` zainstalowany (`package.json:50`), nieużywany.
- `.env.example`: `JWT_SECRET=replace-me`, `REFRESH_TOKEN_PEPPER=replace-me`, `AUTH_DEV_LOGIN_ENABLED=true` (5-9), safe-migrate (70-74). `DEPLOYMENT.md:14-24` minimum prod bez `COOKIDOO_ENCRYPTION_KEY` (crash-loop przy świeżym deployu). CI env (`backend-ci.yml:30-39`): DATABASE_URL, JWT_SECRET=ci-secret, REFRESH_TOKEN_PEPPER=ci-pepper, AUTH_DEV_LOGIN_ENABLED='true', APNS_ENABLED='false', CORS_ORIGIN, COOKIDOO_ENCRYPTION_KEY (32 B base64). CI: Node 22, pnpm 10, tylko PR + push main/master.
- `users.gateway.ts`: 62 `users:me`, **67-70 `users:findAll`**, **72-75 `users:findById`**, **77-80 `users:create`**, 82 prefs get, 87 prefs update, 94 profile update, 101 delete, 106 onboarding. `users.service.ts:92-102` `findAll/findById/create` (pełne wiersze User). Wołane tylko z gatewaya + `commands.txt:25`. iOS używa tylko `users:delete/me/onboarding:complete/preferences:*/profile:update`.
- `scripts/prisma-migrate-deploy-safe.js`: 440-455 rebuild (`SAFE_MIGRATE_REBUILD_DB==='true'` + `CONFIRM==='YES_I_UNDERSTAND'`), 371-379 `DROP SCHEMA public CASCADE`, brak NODE_ENV/host; 415-438 R2 backfill (`!== 'false'` = domyślnie ON, `runSoft`), wołany bezwarunkowo 562; bootstrap 381-413 tylko w rebuild (558); DATABASE_URL czytany tylko 504-509/525; refresh tokeny nigdy nie prune'owane. `Dockerfile:55` CMD `node scripts/prisma-migrate-deploy-safe.js && exec node dist/main`.
- `Dockerfile`: node:20-bookworm-slim ×3 (1,13,24), pełny `pnpm install` (9), runner kopiuje `node_modules` dev (32), `src` (34), `test` (35), `scripts` (36); brak HEALTHCHECK/USER. `.dockerignore`: dist node_modules .git .idea coverage *.log .env. `prisma` (`package.json:79`) i `tsx` (`:87`) devDependencies.
- `/ops/health` bez DB (`ops.controller.ts:30-48`), `/ops/metrics` bez guardu, DB w try/catch. Brak `OPS_TOKEN` gdziekolwiek. Jedyne `@UseGuards`: `integrations.controller.ts:13`, `health-steps.controller.ts:14`.
- `onModuleDestroy` istnieją (`prisma.service.ts:13-15`, `notifications.service.ts:126-129`), nigdy nie biegną.
- `households.service.ts:92-109` `create` bez sprawdzenia członkostwa i bez transakcji; `acceptInvitation:166-186` wymusza jeden dom (`INVITATION_REQUIRES_LEAVE`). Reguła „które gospodarstwo”: najstarsze członkostwo (`auth.service.ts:246-254`, `cookidoo-integration.service.ts:236-252`); WS: `householdId` z payloadu + `ensureMembership`; recipes: opcjonalny `householdId`, bez niego brak kontroli i globalny cache.
- Prod Railway `Backend`: `AUTH_DEV_LOGIN_ENABLED=false`, `JWT_SECRET` 72 zn., `REFRESH_TOKEN_PEPPER` 10 zn., `COOKIDOO_SERVICE_TOKEN` 44, `COOKIDOO_ENCRYPTION_KEY` 44, `SAFE_MIGRATE_BACKFILL_R2_IMAGE_URLS=true`, `JWT_EXPIRES_IN=30d`, brak `NODE_ENV`/`OPS_TOKEN`. Dev `.env`: JWT_SECRET 10, pepper 10, `AUTH_DEV_LOGIN_ENABLED=false`, `WS_CORS_ORIGIN=*`. `docker-compose.yml`: api `env_file: .env` + inline DATABASE_URL/COOKIDOO_SERVICE_URL.

## Kontrakt błędów (C5)
- `app-exception.ts` response `{code,message,details}`; `details` nigdy nie przekazywane. `app-error-code.ts` — nigdy nie rzucane: UNAUTHORIZED, NOT_FOUND, COOKIDOO_SUBSCRIPTION_INACTIVE, INTERNAL_ERROR.
- `ws-response.ts`: STATUS_CODE_MAP 400/401/403/404/409/422/429; nie-HttpException → surowy `error.message` (Prisma!) + INTERNAL_ERROR 500; brak details/requestId. Wywołania: households 16, weekly-plans 14, users 10, recipes 5, notifications 2.
- Zero `APP_FILTER/@Catch/useGlobalFilters`. `request-logging.interceptor.ts:26-28` pomija WS; 34-40 requestId (nagłówek lub randomUUID) na `req.requestId` + nagłówek odpowiedzi, nigdy w body. `request-metrics.service.ts:24-47` liczy błędy tylko ≥500.
- Rzuty: 32 `AppException` vs 49 gołych Nest (auth 13, weekly-plans 8, recipes 5, users 2, households 10, stub 11). Prisma: tylko P2002 (`weekly-plans.service.ts:428-434`) i P2034 retry (`transaction-runner.util.ts`).
- Cookidoo client 76-114: `COOKIDOO_AUTH_FAILED`→409, `RECIPE_NOT_FOUND`→404, `INTERNAL_UNAUTHORIZED`→503, default→503; Python emituje `COOKIDOO_UPSTREAM_ERROR` 502 (`app/cookidoo_client.py:56`). `connect()` nie sprawdza `subscription.active` (klient iOS to robi).
- iOS: `RecipeProtocols.swift:57-82` `WsEnvelope{ok,data,error,code,status}`, `RecipeDataError{invalidRecipeId, transportNotConfigured, serverError(message)}`; `envelope.code` czytany tylko w `WeeklyPlanStore.swift:235` (`NOT_FOUND`) i `SessionStore.swift:1929` (print). 28 miejsc rzuca `serverError(message: envelope.error ?? …)`. `UserFacingErrorMapper.swift` (97 linii): 14 z ~16 gałęzi to podciągi angielskich komunikatów; `isConnectivityIssue` łapie goły `"socket"`. `IntegrationsAPIClient.swift:116-121` `backend(code:status:)`, fallback `401 → "UNAUTHORIZED"`; `IntegrationsDTOs.swift:68-90` `BackendHttpErrorDTO{code?,message?}` (message String lub [String].first); `CookidooIntegrationStore.swift:130-156` maper po kodach (wzór). `RecipeCatalogStore.swift:36-52` cache `recipes_catalog_cache_v11.json`, brak `clear()`; `SessionStore.logout()` (304-315) → `clearRuntimeStores()` (551-573) tylko `recipeCatalogStore = nil`.

## Testy/CI (C6–C8)
- Żaden plik nie jest dataless; `.icloud` brak. `jest.config.js:22-25` maper `households.service` → stub (nieskopowany: dotyka module/gateway). Stub: 5 metod, `prisma.householdInvitation` (nie istnieje; model `Invitation` z `redeemedAt`, `createdById`, `invitedUserId`, `declinedAt`). Spec households: 15 `it`, `$transaction` z nowym mockiem (nie tym samym), `findFirst` zamiast `findUnique`.
- `HouseholdsService` API: `findAll(userId)`, `findById`, `create(userId, dto{name})`, `createInvitation(userId, householdId, dto{expiresAt?})`, `acceptInvitation(userId, dto{token, leaveOtherHouseholds?})`, `previewInvitation`, `listPendingInvitations`, `declineInvitation(userId, dto{token})`, `updateName`, `updateMealTypes` (member-gated), `updateMealTimes`, `listMembers`, `updateMemberRole(userId, householdId, memberUserId, dto{role})`, `removeMember(userId, householdId, memberUserId)`, `leave(userId, householdId)`, `getUserDisplayName`. Hooki: `onMemberLeft(tx, hh, userId, now)`, `onRosterChanged(tx, hh, old, new, now)`.
- `tsconfig.json`: isolatedModules, `rootDir ./src`, include src+test; `tsconfig.build.json` exclude test, `**/*spec.ts`, `**/*.stub.ts`. Brak skryptu `typecheck`, brak `tsconfig.scripts.json`. eslint glob `{src,apps,libs,test}`; lint-staged obejmuje wszystko `*.{ts,js}`.
- Spodziewane błędy `tsc --noEmit`: `weekly-plans.service.spec.ts:216,230,858,870` (`dayOfWeek: 1`; `:33` też poprawić), households spec (~13), stub (~5). Bez flagi `NODE_OPTIONS=--experimental-vm-modules` `apple-identity.service.spec` pada na `import('jose')` (z flagą 8/8).
- e2e `test/smoke.e2e-spec.ts`: 3 testy; boot bez pipe'ów; `POST /auth/dev` z `displayName/email/householdName` (`DevLoginDto` ma `householdName`); brak sprzątania; `test/jest-e2e.json` bez mapera (prawdziwy serwis).
- `scripts/`: 16 `.ts` + `prisma-migrate-deploy-safe.js`; 4 importują `../src`. Zero `AI_ENABLED/ANTHROPIC`.
- Złote wzorce: `suitable-meal-types.util.spec.ts` (ręcznie przepisany `CATALOGUE`, `it.each` po tytule), `department-classifier.util.spec.ts` (iteruje enum). Bez spec-a: `text-normalization.util.ts` (`normalizeProductKey` zachowuje diakrytyki), `shopping-items.util.ts` (`itemSignature` 6 miejsc, display 2 miejsca, dopisek `(jednostka)`, sort `DEPARTMENT_ORDER` + `localeCompare`). `shopping-list.service.ts`: `:100` `normalizedAmount ?? amount`, `:207-235` precedencja zaznaczeń (delta archiwum → `isChecked` → legacy → false; epsilon 0.000001 na niezaokrąglonych vs 2-miejscowy baseline), `:493-603` archiwum (signature reuse vs `revision+1`).
- Katalog: 89 przepisów, 745 wierszy składników, 131 nazw, jednostki g/szczypta/ml/szt/łyżeczka; JSON bez `department`; 17 plików txt = 403 składniki; nutrition 135 wierszy.
