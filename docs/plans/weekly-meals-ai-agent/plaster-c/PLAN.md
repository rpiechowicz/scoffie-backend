# Plaster C — platforma i testy przed Fazą 0

## Kontekst

Plastry A (liczby) i B (semantyka planu) są na prod od 28.08. Plaster C zamyka platformę i testy, na których stanie moduł agenta: sekrety z publicznymi wartościami domyślnymi bez asercji na prod, otwarte handlery `users:*` i `POST /auth/google` (wyciek PII / przejęcie konta), `SAFE_MIGRATE_REBUILD_DB` kasujący schemat przy każdym starcie, obraz prod na Node 20 (EOL), trzy kształty błędów na drucie i surowe komunikaty Prismy w UI, spec `HouseholdsService` testujący stub o innym API, spec-i nigdy nie type-checkowane, e2e bez `ValidationPipe`, `scripts/` poza każdą bramką.

Zweryfikowane w źródle 28.08 (trzy przeglądy): żaden plik nie jest dziś dataless (stub to martwa proteza; `households.service.ts` edytowany w plastrze B); `tsc --noEmit -p tsconfig.json` padnie w dokładnie 3 plikach (~22 błędy); prod: `AUTH_DEV_LOGIN_ENABLED=false` (`POST /auth/dev` → 403), `JWT_SECRET` 72 znaki, **`REFRESH_TOKEN_PEPPER` 10 znaków**, `SAFE_MIGRATE_BACKFILL_R2_IMAGE_URLS=true`, brak `NODE_ENV` w Railway (obraz ustawia `production`); dev `.env`: `JWT_SECRET` i pepper po 10 znaków, `AUTH_DEV_LOGIN_ENABLED=false`; iOS tworzy gospodarstwo wyłącznie, gdy użytkownik żadnego nie ma; iOS nie używa `users:findAll/findById/create` ani `/auth/google`; `DevLoginDto` ma `householdName` (e2e przeżyje `forbidNonWhitelisted`).

Decyzja Rafała: repa zostają w iCloud z „Keep Downloaded” na folderze — stub i maper w jest do usunięcia.

Gałęzie `fix/fundamenty-c` z `origin/develop` (oba repa). Bez tsc/jest/prisma na hoście (kontener), iOS przez `xcodebuild`, commity z `--no-verify`. Szacunek: backend ~3 dni (C5 to sam ~1 dzień — więcej, niż zakładał artefakt), iOS ~1 dzień.

---

## C1. Sekrety fail-fast (~1 h)

- Nowy `src/config/assert-env.ts`: `assertRuntimeEnv(env: NodeJS.ProcessEnv = process.env): void`, czysta funkcja (testowalna). Gdy `env.NODE_ENV === 'production'`: `JWT_SECRET` ≥ 32 znaki i spoza `{replace-me, dev-secret-change-me, ci-secret}`; `REFRESH_TOKEN_PEPPER` ≥ 32 znaki, różny od `JWT_SECRET`; `DATABASE_URL` obecny; `COOKIDOO_SERVICE_TOKEN` niepusty; `COOKIDOO_ENCRYPTION_KEY` przez istniejący `parseEncryptionKey` (`src/common/crypto.util.ts`); `AUTH_DEV_LOGIN_ENABLED !== 'true'`. Poza produkcją: tylko `console.warn`, gdy sekret jest domyślny/krótki. Błąd = jeden `Error` z listą naruszeń (bez wartości). Wołane jako pierwsza linia `bootstrap()` w `src/main.ts`, przed `NestFactory.create`.
- `src/auth/auth.module.ts:13` i `auth.service.ts:47`: fallbacki zostają (dev/CI), ale ich użycie loguje ostrzeżenie raz przy starcie — na prod asercja i tak ich nie dopuszcza.
- `DEPLOYMENT.md` minimum prod: dołożyć `COOKIDOO_ENCRYPTION_KEY`, `COOKIDOO_SERVICE_TOKEN`, `REFRESH_TOKEN_PEPPER` (≥32), usunąć `AUTH_DEV_LOGIN_ENABLED=true`; `README.md` „always required” to samo; `.env.example`: komentarz `openssl rand -base64 32`.
- Test: `src/config/assert-env.spec.ts` — tabela: prod + komplet → OK; prod + brak/krótki/domyślny sekret → throw z nazwą zmiennej; prod + `AUTH_DEV_LOGIN_ENABLED=true` → throw; `development` + braki → brak throw.
- Skutek operacyjny (runbook niżej): na Railway rotować `REFRESH_TOKEN_PEPPER` do ≥32 znaków **przed** merge (unieważnia refresh tokeny — iOS ich dziś nie używa, więc nikt tego nie zauważy); dev `.env`: nowe `JWT_SECRET`/`REFRESH_TOKEN_PEPPER` (`openssl rand -base64 32`), bo obraz dev też ma `NODE_ENV=production` — dev-apka wymaga jednego ponownego logowania.

## C2. Martwe i niebezpieczne handlery (~1 h)

- `src/users/users.gateway.ts`: usunąć `users:findAll`, `users:findById`, `users:create` + `UsersFindByIdPayload`, import `CreateUserDto`; `src/users/users.service.ts`: usunąć `findAll`, `findById`, `create`; skasować `src/users/dto/create-user.dto.ts`; `commands.txt:25` → `users:me`.
- `POST /auth/google` (F2): usunąć trasę w `auth.controller.ts:14-25`, `AuthService.loginWithGoogle`, `src/auth/dto/google-oauth.dto.ts`, 4 przypadki w `auth.service.spec.ts:204-262`. `googleId` w schemacie zostaje (dev-login i użytkownicy legacy go używają).
- Dev-login opt-in (F3): `auth.service.ts:174` → `if (process.env.AUTH_DEV_LOGIN_ENABLED !== 'true') throw …`; `auth.service.spec.ts` — `beforeEach` bez zmian (ustawia `'true'`), dołożyć przypadek „nieustawione → Forbidden”, zmienić `:171` na „`false` → Forbidden” (bez zmian semantyki testu). `.env.example:9` zostaje `true` (dev), CI ma `'true'`, prod ma `false`. Kod błędu: `DEV_LOGIN_DISABLED` (C5).
- Test: `src/users/users.service.spec.ts` — `it.each` „metoda nie istnieje” dla `findAll/findById/create` (wzór z `weekly-plans.service.spec.ts`); `auth.service.spec.ts` — `loginWithGoogle` nie istnieje.

## C3. Bezpieczny safe-migrate (~1 h)

- Nowy `scripts/lib/rebuild-guard.js` (CJS, czysta funkcja): `decideRebuild({ env, now }) → { allowed: boolean, reason: string, host: string | null }`. Reguły: `SAFE_MIGRATE_REBUILD_DB === 'true'`, **`SAFE_MIGRATE_REBUILD_CONFIRM` musi równać się dzisiejszej dacie UTC `YYYY-MM-DD`** (samowygasające; `YES_I_UNDERSTAND` przestaje działać), host z `DATABASE_URL` (`new URL(...)`, `host` bez hasła); gdy `NODE_ENV === 'production'` dodatkowo `SAFE_MIGRATE_ALLOW_PROD_REBUILD === host`. `prisma-migrate-deploy-safe.js:445-455` używa guardu, loguje `host` przed `DROP SCHEMA` i wychodzi z kodem 1 z czytelnym powodem.
- R2 backfill: `runOptionalR2ImageBackfill` opt-in (`SAFE_MIGRATE_BACKFILL_R2_IMAGE_URLS === 'true'`), `.env.example:72` → `false`; `commands.txt`: jednorazowe `docker compose exec api pnpm exec tsx scripts/backfill-recipe-image-urls-from-r2.ts`.
- `jest.config.js`: transform tylko `^.+\.ts$` (żeby jest nie przepuszczał CJS przez ts-jest); test `src/config/rebuild-guard.spec.ts` (`require('../../scripts/lib/rebuild-guard.js')`): brak flagi → nie; flaga + stara data → nie; flaga + dziś → tak (dev); prod + dziś bez hosta → nie; prod + dziś + host zgodny → tak; `DATABASE_URL` bez hasła w `host`.

## C4. Obraz prod (~1 h)

- `Dockerfile`: `node:22-bookworm-slim` we wszystkich trzech stage'ach (zgodnie z CI i README); `HEALTHCHECK --interval=30s --timeout=5s --start-period=40s --retries=3 CMD node -e "fetch('http://127.0.0.1:'+(process.env.PORT||3000)+'/ops/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"`; `RUN chown -R node:node /app` + `USER node` przed `CMD`; nie kopiować `test/` do runnera (jest nie biega w obrazie; `Dockerfile:35` out). `src/` zostaje (skrypty `tsx` importują `../src`). `prisma`/`tsx` zostają w devDependencies (pełne `node_modules` w runnerze — świadomie, bez `--prod`).
- `.dockerignore`: `+ .github .husky *.md coverage/ .DS_Store`.
- Weryfikacja: `docker compose up -d --build api` + `docker inspect --format '{{.State.Health.Status}}' weeklymeals-api` → `healthy`; `docker exec weeklymeals-api id -u` → 1000; `docker exec weeklymeals-api node -v` → v22.

## C5. Jeden kontrakt błędów — backend + iOS (~1 dzień + 0,5 dnia)

Kształt na drucie. HTTP (każdy status ≥ 400): `{ code, message, details?: string[], requestId }` — bez `statusCode`/`error`; nagłówek `x-request-id` zawsze. WS (`ok:false`): zostaje `{ ok, error, code, status }` (28 miejsc w iOS czyta `error`), dochodzą `message` (= `error`), `details?`, `requestId`.

**Backend**

- `src/common/app-error-code.ts` — jedna unia (kody generyczne zostają w niej), dochodzą: `BAD_REQUEST`, `TOO_MANY_REQUESTS`, `SERVICE_UNAVAILABLE`, `NOT_HOUSEHOLD_MEMBER`, `OWNER_REQUIRED`, `HOUSEHOLD_NOT_FOUND`, `HOUSEHOLD_ALREADY_MEMBER`, `MEMBER_NOT_FOUND`, `LAST_OWNER`, `INVITATION_NOT_FOUND`, `APPLE_IDENTITY_INVALID`, `DEV_LOGIN_DISABLED`, `RECIPE_NOT_FOUND`, `PLAN_ITEM_NOT_FOUND`, `PLAN_SLOT_DUPLICATE`, `COOKIDOO_UPSTREAM_ERROR`. `AppException.details` zawężone do `string[]` (iOS dekoduje `[String]?`).
- Nowy `src/common/error-contract.ts`: `mapError(error): { contract: { code, message, status, details? }, log: { level, message, stack? } | null }` + `INTERNAL_ERROR_MESSAGE` (stały polski komunikat) + `STATUS_CODE_MAP` (przeniesiony z `ws-response.ts`, + 503). Gałęzie w kolejności: `AppException` → własne pola; inny `HttpException` → tablica `message` przy 400 = `VALIDATION_ERROR` + `details`, inaczej kod ze statusu (`HTTP_ERROR` dla nieznanych); `Prisma.PrismaClientKnownRequestError` P2002→`CONFLICT` 409, P2025→`NOT_FOUND` 404, P2003→`VALIDATION_ERROR` 400 (komunikaty stałe, `log.level='warn'` z `code`/`meta`); obiekt http-errors (`statusCode` 400–599 z body-parsera) → kod ze statusu; reszta (także rzut nie-`Error`, `PrismaClientValidationError`) → `INTERNAL_ERROR` 500, komunikat zamaskowany, oryginał + stack w `log`.
- Nowy `src/common/app-exception.filter.ts` (`@Catch()`), rejestrowany przez `APP_FILTER` w `src/common/common.module.ts` (moduł globalny → działa też w `@nestjs/testing`): kontekst nie-HTTP → `BaseWsExceptionFilter` (siatka bezpieczeństwa; wszystkie 47 handlerów i tak idzie przez `wsRespond`); `requestId = req.requestId ?? nagłówek x-request-id ?? randomUUID()` — **filtr musi sam generować id, bo guardy (`JwtAuthGuard`) odrzucają przed interceptorem**; ustawia nagłówek, `res.status().json(body)`, loguje `mapped.log` z metodą, ścieżką, statusem, kodem i `requestId`. `ValidationPipe` zostaje jako `useGlobalPipes` w `configureApp` (nie `APP_PIPE` — to objęłoby gatewaye i `forbidNonWhitelisted` zaczęłoby odrzucać payloady WS).
- `src/common/ws-response.ts`: `wsRespond(action, meta?: { event? })` używa `mapError`; `WsError = { ok:false, error, message, code, status, details?, requestId }`; `requestId = randomUUID()` per nieudany ack, log jak w filtrze; `setWsErrorObserver(fn)` → `RequestMetricsService.recordWsError(code, status)` (`wsErrors: { total, byCode }` w `snapshot()`), podpięte w `ObservabilityModule`. 47 wywołań bez zmian.
- `request-logging.interceptor.ts`: `finalize` czyta `res.statusCode` zanim filtr go ustawi (rzucone błędy HTTP liczą się jako 200) — `tap({ error })` z `mapError(e).contract.status`; potwierdzić w e2e, że po 401 rośnie `statuses['4xx']`.
- Konwersje rzutów (tekst komunikatów **bez zmian** — stare iOS dopasowuje podciągi): `apple-identity.service.ts:101,104,118,128,134` → `APPLE_IDENTITY_INVALID` 401; `auth.service.ts:175` → `DEV_LOGIN_DISABLED` 403; `households.service.ts:42` → `HOUSEHOLD_NOT_FOUND`, `:52` → `NOT_HOUSEHOLD_MEMBER`, `:60,118` → `OWNER_REQUIRED`, `:141,466` → `INVITATION_NOT_FOUND`, `:593,625` → `MEMBER_NOT_FOUND`, `:603,631` → `LAST_OWNER`, `create()` → nowy strażnik `HOUSEHOLD_ALREADY_MEMBER` 409 (C9); `weekly-plans/utils/auth-checks.util.ts:16` → `RECIPE_NOT_FOUND`, `:33` → `NOT_HOUSEHOLD_MEMBER`; `recipes.service.ts:209` → `NOT_HOUSEHOLD_MEMBER`, `:621,710` → `RECIPE_NOT_FOUND`; `weekly-plans.service.ts:561` → `PLAN_ITEM_NOT_FOUND`, `:431` `CONFLICT` → `PLAN_SLOT_DUPLICATE` 409; `cookidoo-service.client.ts` — `case 'COOKIDOO_UPSTREAM_ERROR'` → 502 `BAD_GATEWAY` „Cookidoo odpowiedziało błędem.” (default nadal 503). Celowo gołe: `jwt-auth.guard.ts` (401→`UNAUTHORIZED`), refresh `auth.service.ts:218`, `weekly-plans.service.ts:162,547` „Weekly plan not found” (iOS `WeeklyPlanStore.swift:235` liczy na `NOT_FOUND` dla pustego tygodnia), `shopping-list.service.ts:623,668,789`, `users.service.ts:167,452`, `recipes.service.ts:200,290`.
- Spec-i po konwersji: `apple-identity.service.spec.ts:101-141` i `auth.service.spec.ts:175` z `rejects.toThrow(UnauthorizedException|ForbiddenException)` → `rejects.toMatchObject({ status, response: { code } })` (`AppException` dziedziczy po `HttpException`, nie po konkretnych klasach); `weekly-plans.service.spec.ts:808` `CONFLICT` → `PLAN_SLOT_DUPLICATE`; przejrzeć `grep -rn "toThrow(\(Forbidden\|NotFound\|BadRequest\)Exception)"` w spec-ach households/recipes.
- Testy: `src/common/error-contract.spec.ts` (tabela 14 przypadków: AppException z/bez `details`, goły 401/403/404/409/429/503, ValidationPipe z tablicą, 418 → `HTTP_ERROR`, `InternalServerErrorException` → komunikat zamaskowany, Prisma P2002/P2025/P2003/P2034, `Error` z SQL w treści → zamaskowany + `log.stack`, rzut stringa/undefined, obiekt http-errors 413/400); `src/common/ws-response.spec.ts` (8: sukces, AppException z `requestId` UUID, goły 404, ValidationPipe, Prisma bez tekstu Prismy w `error`, `Error` → `INTERNAL_ERROR` + `Logger.error` z `requestId`, rzut nie-Error, observer wołany tylko przy błędzie); `src/common/app-exception.filter.spec.ts` (`Test.createTestingModule` + supertest z mini-kontrolerem `@Controller('t')`: 403 body dokładnie `{code,message,requestId}`, `VALIDATION_ERROR` + `details`, goły 404, `Error` → 500 zamaskowany, echo `x-request-id`, P2025 → 404); e2e: reużyty refresh token → body `{ code:'UNAUTHORIZED', message:'Invalid or expired refresh token', requestId }`, `POST /auth/dev {}` → `VALIDATION_ERROR` z `details` zawierającym `displayName`, WS `weeklyPlans:getByWeek` z obcym `householdId` → `{ ok:false, code:'NOT_HOUSEHOLD_MEMBER', status:403, message === error, requestId }`, `/ops/metrics` `http.wsErrors.byCode.NOT_HOUSEHOLD_MEMBER ≥ 1`.

**iOS**

- `Networking/Recipes/RecipeProtocols.swift`: `WsEnvelope` + `message`, `requestId`, `details: [String]?` (pobłażliwe `init(from:)` — obcy kształt `details` nie może położyć całej koperty); `RecipeDataError.server(code:message:status:requestId:)` (`errorDescription` = `message`); `serverError(message:)` zostaje dla transportu (`SocketIORecipeSocketClient`, `WeeklyPlanStore:213/413/429/445`, `SessionStore:992-1060`, `RecipeCatalogStore:258`, `ApiRecipeRepository:25`). Jeden helper: `extension WsEnvelope { func serverFailure(fallback:) -> RecipeDataError?; func requireData(fallback:) throws -> T }` (stary backend bez `message` → bierze `error`).
- Mechaniczna podmiana 28 miejsc na `try envelope.requireData(fallback:)`: `WebSocketRecipeTransportClient.swift:37,57,79`; `WebSocketShoppingListTransportClient.swift:61,99,124,146,167,188,209`; `WeeklyPlanStore.swift:203,239 (po istniejącym `code == "NOT_FOUND"`),279,305,330,347`; `SessionStore.swift:894,927,972,1007,1043,1081,1094`; `:1332` → `serverFailure`; `:339,1694` (`authError = envelope.error ?? …`) → przez maper; `:262-270` (REST Apple) → `.server` z `BackendHttpErrorDTO`, `decodeErrorMessage` (`:296`) do usunięcia; `:1929` print + `requestId`.
- `Models/Stores/UserFacingErrorMapper.swift`: `code(from:)` (`.server`, `IntegrationsAPIError.backend`), tabela `copyByCode` (wszystkie kody wyżej + `INVITATION_*`, `PLAN_*`, `SHOPPING_LIST_*`, `CONFLICT`, `COOKIDOO_*` z `UPSTREAM_ERROR` i `SUBSCRIPTION_INACTIVE`, `UNAUTHORIZED/FORBIDDEN/NOT_FOUND/BAD_REQUEST/VALIDATION_ERROR/INTERNAL_ERROR/TOO_MANY_REQUESTS/SERVICE_UNAVAILABLE`; istniejące polskie stringi przenoszą się dosłownie); `message(from:)` = kod → kopia, dopiero potem stare podciągi (jedno wydanie); `isConnectivityIssue` jako `switch`: `.server` → **false** (goły podciąg `"socket"` nie może dotykać kodów serwera — `ConnectivityErrorGate` opóźniałby o 2 s prawdziwe błędy), `.serverError` → stare podciągi, `.transportNotConfigured`/`IntegrationsAPIError.network`/`URLError` → true.
- `IntegrationsAPIClient.swift`: `backend(code:status:message:)` (+ `decoded?.message`); `IntegrationsDTOs.swift` `BackendHttpErrorDTO` + `requestId`, `details`; `CookidooIntegrationStore.swift`: `case "COOKIDOO_UPSTREAM_ERROR"` + `default` → `UserFacingErrorMapper.message(from:)`. `ConnectivityErrorGate`, `SocketIORecipeSocketClient`, widoki — bez zmian.
- Kompatybilność: stary iOS + nowy backend czyta `error`/`code`/`status` (teksty identyczne, `INTERNAL_ERROR` po polsku zamiast tekstu Prismy, `code == "NOT_FOUND"` dla tygodnia zachowane); nowy iOS + stary backend spada na `error` i stare podciągi. Rollback backendu = wykreślenie `APP_FILTER` (reszta addytywna).

## C6. Prawdziwy spec `HouseholdsService` (~4 h)

Kolejność nośna: **najpierw** nowy spec na prawdziwych sygnaturach, **potem** `git rm src/households/households.service.stub.ts` i usunięcie `moduleNameMapper` z `jest.config.js` — odwrotnie zostawia czerwony `pnpm test`.

- `src/households/households.service.spec.ts` przepisany od zera na wzór `weekly-plans.service.spec.ts` (`makePrismaMock` z `const mock: any`, `$transaction` oddający **ten sam** mock; delegaty `household`, `membership` (`findUnique/findMany/count/create/upsert/update/delete`), `invitation`, `user`, `weeklyPlan`, `planItem`, `planItemParticipant`, `planItemConsumption`, `shoppingList` — hooki `plan-roster.util` biegną naprawdę). Przypadki (~25): `create` (transakcja: household + membership OWNER; **drugie członkostwo → `HOUSEHOLD_ALREADY_MEMBER` 409** — C9); `createInvitation` (nie-członek → `NOT_HOUSEHOLD_MEMBER`, członek nie-właściciel → `OWNER_REQUIRED`, właściciel → `invitation.create` z tokenem i `expiresAt`); `acceptInvitation` (nieznany token → `INVITATION_NOT_FOUND`, `redeemedAt` → `INVITATION_ALREADY_REDEEMED`, po terminie → `INVITATION_EXPIRED`, `declinedAt` → `INVITATION_DECLINED`, inne członkostwo bez `leaveOtherHouseholds` → `INVITATION_REQUIRES_LEAVE` 409, z flagą → delete starego + `onMemberLeft` + upsert + `invitation.update(redeemedAt, invitedUserId)`, liczniki wokół upsertu → `onRosterChanged` tylko gdy skład się zmienił, `touchedWeeks`, kolor awatara przy kolizji); `declineInvitation`; `updateMealTypes` (członek może; sloty bazowe zawsze zostają — `normalizeEnabledMealTypes`); `updateMemberRole` (ostatni właściciel → `LAST_OWNER`); `removeMember` (nie-właściciel → `OWNER_REQUIRED`, ostatni właściciel → `LAST_OWNER`, sukces → `settleHouseholdAfterMemberLeft` + `onMemberLeft` z `memberUserId`, `touchedWeekStarts`); `leave` (bez blokady ostatniego właściciela, `householdDeleted` gdy pusto — hook pominięty); `listMembers` (nie-członek → 403).
- Nowy `src/households/households.gateway.spec.ts` (wzór `weekly-plans.gateway.spec.ts`): `emitMembersChanged` z listą i bez listy (gdy `listMembers` rzuca po `leave`), `emitPlanTouched` = para `weekChanged` + `shoppingListChanged` z `MEMBERSHIP_CHANGED` per tydzień, `removeMember`/`leave`/`acceptInvitation` rozgłaszają dotknięte tygodnie.
- Komentarz w `jest.config.js` po usunięciu mapera: dlaczego go nie ma (iCloud „Keep Downloaded”; przy EIO materializować `cat > /dev/null`, nie wracać do stubu).

## C7. Type-check spec-ów w CI (~1 h)

- `package.json`: `"typecheck": "tsc --noEmit -p tsconfig.json"` (`--noEmit` konieczne: `rootDir: ./src` vs `include: test/**`).
- `.github/workflows/backend-ci.yml`: krok `Typecheck` po `Generate Prisma client`, przed `Build`; `on.push.branches` + `develop`.
- Naprawa dryfu: `weekly-plans.service.spec.ts` `dayOfWeek: 1` → `'MON'` w `:33, :216, :230, :858, :870`; reszta wychodzi z C6 (spec households + skasowany stub). Oczekiwane po zmianie: 0 błędów (weryfikacja w kontenerze `npx tsc -p tsconfig.json --noEmit`).

## C8. e2e, złote testy, `scripts/` w bramkach (~4 h)

- Nowy `src/app.setup.ts`: `configureApp(app: NestExpressApplication)` — CORS (bez zaszytego `localhost:5173` na prod: tylko z `CORS_ORIGIN`, w dev/test dołożony), `ValidationPipe {whitelist, forbidNonWhitelisted, transform}`, `app.set('trust proxy', 1)`, `enableShutdownHooks()`, static assets. `main.ts` = `assertRuntimeEnv()` → `create` → `configureApp` → `listen`; `bootstrap().catch(...)` z `process.exit(1)`. `test/smoke.e2e-spec.ts` woła `configureApp(app)` po `createNestApplication()`.
- e2e (`test/smoke.e2e-spec.ts`): nagłówek `x-ops-token` przy `/ops/metrics` (CI env `OPS_TOKEN: ci-ops-token`) + przypadek 403 bez nagłówka; dwa asserty kształtu błędu z C5; sprzątanie w `afterAll` (usunięcie utworzonego gospodarstwa i przepisu — kaskady zabiorą resztę); `DevLoginDto` z `householdName` przechodzi `forbidNonWhitelisted` (zweryfikowane).
- `scripts/` w bramkach: `tsconfig.scripts.json` (`extends`, `rootDir: "."`, `noEmit`, `include: ["scripts/**/*.ts", "src/**/*.ts"]`, `exclude: spec/stub/test`) + skrypt `"typecheck:scripts"` + krok CI; `lint:check`/`lint` glob `{src,apps,libs,test,scripts}`; naprawić, co wyjdzie (spodziewane: nieużywane zmienne / `any` w skryptach — tylko warningi poza `no-unused-vars`).
- Złote testy (wzór `suitable-meal-types.util.spec.ts`: ręcznie przepisana tabela z prawdziwych nazw katalogu, `it.each` po tytule):
  - nowy `src/weekly-plans/utils/text-normalization.util.spec.ts`: `normalizeProductKey` (trim/lower, zachowuje diakrytyki, `::` separator), `toTitleCase`, `keywordMatches` (rdzeń, granica słowa), `escapeForRegex`;
  - nowy `src/weekly-plans/utils/shopping-items.util.spec.ts`: `itemSignature` niezależny od kolejności i wrażliwy na ilość/jednostkę/dział; `buildDisplayShoppingItems` — zaokrąglenie do 2 miejsc, `isChecked` z mapy, dopisek `(jednostka)` przy tej samej nazwie w dwóch jednostkach, kolejność działów wg `DEPARTMENT_ORDER` + `localeCompare` w dziale;
  - `shopping-list.service.spec.ts` +5: przeniesienie zaznaczeń przy przebudowie (`ShoppingListItem.isChecked` > legacy `ShoppingItemCheck` > `false`), wymuszone odznaczenie przy wzroście ilości względem bieżącego archiwum (epsilon `0.000001` — porównanie po zaokrągleniu do 2 miejsc, bo baseline z archiwum jest 2-miejscowy: poprawka w `rebuildShoppingListSnapshot:221-225`), `normalizedAmount: null` → pozycja wchodzi w surowej ilości/jednostce **i** `Logger.warn` z nazwą (nie po cichu), `archiveShoppingList`: ten sam zestaw → to samo archiwum bez podbicia `revision`, zmieniony → `max+1`, `SHOPPING_LIST_EMPTY`/`SHOPPING_LIST_NOT_COMPLETED`;
  - złota tabela 12 prawdziwych składników katalogu (nazwa, jednostka po normalizacji, dział z pliku `ingredients-*.txt`) → `productKey` + `toShoppingDepartment` + `toTitleCase` — w `text-normalization.util.spec.ts`.

## C9. Drobiazgi (~1,5 h backend + 0,5 h iOS)

- `/ops/metrics`: guard `OpsTokenGuard` (nagłówek `x-ops-token` = `OPS_TOKEN`); gdy `OPS_TOKEN` nieustawiony: w `production` → 403 zawsze (i asercja C1 wymaga `OPS_TOKEN`), poza produkcją → otwarte. `/ops/health` bez zmian. `commands.txt`: przykład `curl -H "x-ops-token: …"`.
- `enableShutdownHooks()` (w `configureApp`) — uruchamia istniejące `PrismaService.onModuleDestroy` i `NotificationsService.onModuleDestroy`.
- `HouseholdsService.create`: w jednej transakcji; istniejące członkostwo → `AppException('HOUSEHOLD_ALREADY_MEMBER', …, 409)` (iOS pokazuje kreator tylko bez gospodarstwa — brak wpływu). „Które gospodarstwo”: bez zmian kodu, notatka w komentarzu `auth.service.ts buildAuthResult` (najstarsze członkostwo; docelowo JWT po auth WS w Fazie 0).
- iOS: `RecipeCatalogStore.clearCache()` (usuwa `recipes_catalog_cache_v11.json`) wołany z `SessionStore.clearRuntimeStores()` przy wylogowaniu — dziś katalog poprzedniego konta żyje 12 h po zmianie użytkownika.

---

## Kolejność commitów

Krok 0: zapisać trzy raporty zwiadu i projekt C5 do `~/.claude/plans/weekly-meals-ai-agent/plaster-c/` (jak przy B), utworzyć gałęzie z `origin/develop` po `git fetch --prune`.

**Backend `fix/fundamenty-c`** (jeden PR): C2 → C1 → C3 → C4 → C9-backend (guard ops, shutdown hooks, `households.create`) → C6 → C7 (od tego commitu CI ma type-check) → C8 → C5 (najszerszy — na końcu, gdy reszta jest zielona). Po każdym: `rm -rf /app/src /app/test` → `docker cp` → `NODE_OPTIONS=--experimental-vm-modules npx jest …` w kontenerze.
**iOS `fix/fundamenty-c`**: C5 (kontrakt) → C9 (cache przy wylogowaniu).

## Wdrożenie (runbook prod — kolejność nośna)

1. **Przed merge do `main`**, na Railway `Backend` (`--skip-deploys`): `REFRESH_TOKEN_PEPPER` = nowe 32+ znaki; `OPS_TOKEN` = nowe 24+ znaki; usunąć `SAFE_MIGRATE_BACKFILL_R2_IMAGE_URLS` (domyślnie off); upewnić się `AUTH_DEV_LOGIN_ENABLED=false` (jest). Bez tego pierwszy start po deployu padnie na asercji C1 (crash-loop — Railway zostawi stary kontener? nie liczyć na to).
2. Merge backend → `develop` → `main`; `/ops/health` z nowym commitem; `/ops/metrics` bez nagłówka → 403, z nagłówkiem → 200; `curl -X POST /auth/google` → 404; ws-smoke `users:findAll` → brak ACK.
3. Dev: `.env` nowe sekrety (`openssl rand -base64 32`), `docker compose up -d --build api`, `docker inspect` → healthy.
4. iOS → `develop` → `main` → TestFlight (może iść niezależnie; nowy iOS działa też na starym backendzie).
5. Żadnego SQL. Rollback = revert kodu; zmienne Railway mogą zostać.

## Weryfikacja

- Kontener: `NODE_OPTIONS=--experimental-vm-modules npx jest` (tak jak `pnpm test`; bez tej flagi `apple-identity.service.spec` pada na dynamicznym `import('jose')` — to były „3 znane porażki” z plastra B, nie błąd kodu; z flagą 8/8) → cel 0 porażek; `npx tsc -p tsconfig.json --noEmit` → 0 błędów, `npx tsc -p tsconfig.scripts.json --noEmit` → 0, `npx eslint "{src,test,scripts}/**/*.ts"` bez errorów, e2e `-e AUTH_DEV_LOGIN_ENABLED=true -e OPS_TOKEN=ci-ops-token` → 3+ pass; `docker compose up -d --build api` → `healthy`, `node -v` 22, uid 1000.
- Smoke WS na dev: `households:create` drugi raz → `HOUSEHOLD_ALREADY_MEMBER` 409; `weeklyPlans:upsertWeekSlot` obcego usera → `{code:'NOT_HOUSEHOLD_MEMBER', status:403, requestId}`; `households:acceptInvitation` zły token → `INVITATION_NOT_FOUND`; wymuszony błąd Prismy (np. `weeklyPlans:setMealEaten` na nieistniejącym itemie) → `INTERNAL_ERROR`/`NOT_FOUND` bez tekstu Prismy w `error`.
- Symulacja `SAFE_MIGRATE_REBUILD_DB=true` w jednorazowym kontenerze bez `CONFIRM` = dziś → skrypt kończy się kodem 1 i loguje host, schemat nietknięty.
- iOS: `xcodebuild` + ręcznie: dołączenie do cudzego slotu przez ws-smoke z id telefonu → banner „Nie należysz do tego gospodarstwa”; tryb samolotowy → „Problem z połączeniem na żywo” (transport, nie kod); wylogowanie → plik `recipes_catalog_cache_v11.json` znika; zaproszenie po terminie → „To zaproszenie wygasło”.
