# Archiwum katalogu

Pliki historyczne — NIE do importu. Cztery są bez identyfikatorów przepisów
(import zakończyłby się błędem), a `recipes-db-v1-import.json` ma jeden
identyfikator przypisany do innego tytułu niż w katalogu. Źródłem prawdy
jest `../recipes-catalog-full-v2.json`; partie (`recipes-batch-*`) to jego
składowe.
